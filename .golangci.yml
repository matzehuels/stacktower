version: "2"

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/matzehuels/stacktower

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - misspell
    - unconvert

  settings:
    staticcheck:
      checks:
        - all
        - -ST1000
        - -ST1016
        - -QF1003
